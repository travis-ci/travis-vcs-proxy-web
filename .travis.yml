---
language: node_js
node_js:
  - "18"

dist: focal

addons:
  chrome: stable

cache:
  directories:
    - $HOME/.npm

env:
  global:
    # See https://git.io/vdao3 for details.
    - JOBS=1
    - NODE_OPTIONS=--no-experimental-fetch

branches:
  only:
    - master

script:
  - npm test
