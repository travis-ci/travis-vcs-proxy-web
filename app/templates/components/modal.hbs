<AnimatedIf
  @predicate={{@isVisible}}
  @use={{this.transition}}
  @duration={{this.animationDuration}}
>
  <div
    class="travis-modal-overlay fixed inset-0 z-50 flex items-center justify-center overflow-hidden bg-grey-800-fog"
    {{on 'click' (action this.onClickOverlay)}}
  >
    <div
      class="travis-modal bg-white p-8 relative rounded-sm"
      {{on 'click' (action this.onClickModal)}}
    >
      {{~yield~}}
      {{#if this.closeButton}}
        <button
          type="button"
          title="Close modal"
          class="absolute cursor-pointer top-0 right-0 p-0 mt-8 mr-8 no-print"
          {{on 'click' this.onClose}}
        >
          <SvgImage @name="flash-close" @class="w-5 h-5" />
        </button>
      {{/if}}
    </div>
  </div>
</AnimatedIf>