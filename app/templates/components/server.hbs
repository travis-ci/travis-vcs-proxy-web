<div class="servers-page-header">
  <div class="left-bar">
    <div class="page-path">
      VCS Servers / {{this.args.server.name}} / <span class="page-path-active">{{page-title}}</span>
    </div>
    <UserAvatar
      @name={{this.user.name}}
      @size={{40}}
      class="navigation-avatar"
    />
    <span class="user-name">{{this.user.name}}</span>
  </div>
</div>
{{outlet}}
