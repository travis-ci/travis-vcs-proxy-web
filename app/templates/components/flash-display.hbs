<ul class='{{this.classes}}'>
  {{#let (get this.messages "0") as |flash|}}
    {{#if flash}}
      <FlashItem
        @flash={{flash}}
        @close={{action "closeMessage"}}
      />
    {{/if}}
  {{/let}}
</ul>